app.directive('buttonShow', [function(){
	// Runs during compile
	return {
		templateUrl: '/directives/buttonDirectives.html'
    // controller: ['$scope', '$interval', function($scope, $interval) {

 
    // }]
  // };
	};
}]);